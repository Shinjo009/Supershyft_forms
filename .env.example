# Local: copy to .env, then restart the dev server (npm run dev).
# Vercel: Project Settings → Environment Variables (Production + Preview).

VITE_BACKEND_BASE_URL=https://api.supershyft.com

# Engagement code used by /users/code/{code}/onboard/me
VITE_ENGAGEMENT_CODE=SUMU0226
